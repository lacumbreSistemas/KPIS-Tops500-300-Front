import { SearchProductosPasillosPipe } from './search-productos-pasillos.pipe';

describe('SearchProductosPasillosPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProductosPasillosPipe();
    expect(pipe).toBeTruthy();
  });
});
